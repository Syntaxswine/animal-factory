# Coordinator review — 17 September 2026

## Gameplay and fire animation

Approved `tactics-playtest-followups` through `64cd0c5` and `tactics-ground-fire` through `c1a74ac`, with integration corrections. Recovery, local alerts, searchable bodies, automated squad behavior and ground-fire animation are included. Overwatch rendering and existing demo assets are preserved in the two merge resolutions.

Independent probes found two gaps, corrected here:

- A second defeat on a revisited map changed an earlier captive's roster status to dead. Terminal casualty states now survive later defeats; the existing repeated-retreat test checks the roster as well as the historical loss records.
- The bot treated every exploration-phase reload as free, despite active alerts retaining AP costs. Weapon readiness now uses the engine's combat-cost predicate. A regression checks an empty held gun with reserve ammunition and zero AP during a real-time alert.

Validation: 392 tests and asset checks passed; Pages distribution built; browser game and flame gallery loaded without resource or JavaScript errors. Three independent factory runs (1947–1949) finished with wins and no stalls. This is a smoke sample, not a new twenty-seed balance certification. Source integration does not publish the separate Pages repository.

## Graphics checkpoint

Reviewed `sprite-migration` through `f8217c6`, including its supplied matched images, pose sheet and live representative room. Its 365 tests and asset checks pass. The room loads without browser/resource errors, retains a measured 2:1 projection, and its doorway/window/wall/roof query controls run.

The material and prop direction is approved for continued development: richer brickwork and ground surfaces, distinct caps, cylindrical barrels, painted foliage and intact character artwork address the previous visual downgrade. Keep this direction and improve the remaining details; another style restart is unnecessary.

The full migration branch remains unmerged. Its rigid presentation is limited to the review viewer; the game still uses the earlier calibrated/deformed sprite path. The reported rigid muzzle displacement is 0.208–0.633 tiles against the existing 0.05-tile requirement. Passing tests of the separate calibrated path do not resolve that mismatch. Directional armed artwork or an explicitly accepted interim facing constraint is still required before production sprite approval. Do not relax the tolerance or reintroduce visible deformation to hide it.

Next handoff: demonstrate the chosen directional solution with intact anatomy and matching physical muzzle origins, then carry the approved room materials and prop treatment into the opt-in map renderer and editor. Preserve the canonical default while those gates remain open. Water animation and representative full-map performance remain outstanding; a short room timing sample does not establish either.
