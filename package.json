{
  "name": "animal-factory",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "A farm logistics game prototype.",
  "scripts": {
    "serve": "node tools/serve.mjs",
    "check": "node --check dist/app.js && node --check dist/engine.js && node --check dist/renderer.js && node --check dist/people.js && node --check dist/isometric.js && node --test tests/*.test.mjs && node tools/check-assets.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
